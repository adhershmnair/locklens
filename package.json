{
  "name": "locklens",
  "displayName": "LockLens",
  "description": "Inline resolved versions from lock files across npm, yarn, pnpm, bun, and composer.",
  "version": "0.2.0",
  "publisher": "adhershmnair",
  "icon": "media/logo.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/adhershmnair/locklens"
  },
  "bugs": {
    "url": "https://github.com/adhershmnair/locklens/issues"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "npm",
    "yarn",
    "pnpm",
    "bun",
    "composer",
    "lockfile",
    "versions",
    "dependencies"
  ],
  "activationEvents": [
    "onLanguage:json",
    "onLanguage:jsonc"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "locklens.refresh",
        "title": "LockLens: Refresh resolved versions"
      },
      {
        "command": "locklens.toggle",
        "title": "LockLens: Toggle inline versions"
      }
    ],
    "configuration": {
      "title": "LockLens",
      "properties": {
        "locklens.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show inline resolved versions."
        },
        "locklens.showOnlyIfDiffers": {
          "type": "boolean",
          "default": false,
          "description": "Only show resolved version when it differs from the declared constraint."
        },
        "locklens.checkUpdates": {
          "type": "boolean",
          "default": true,
          "description": "Check the registry (npmjs / Packagist) for newer versions and color-code accordingly."
        },
        "locklens.outdatedColor": {
          "type": "string",
          "default": "#d64545",
          "description": "Color used when a newer version is available on the registry."
        },
        "locklens.upToDateColor": {
          "type": "string",
          "default": "#64a46b",
          "description": "Color used when the resolved version matches the latest on the registry."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package",
    "publish": "vsce publish"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^2.22.0",
    "typescript": "^5.3.0"
  }
}
